# 安全策略

## 支持版本

下表说明本项目各版本的安全更新支持情况：

| 版本 | 支持状态 |
|------|----------|
| 1.0.x | ✅ 活跃支持 |
| < 1.0 | ❌ 不再支持 |

## 报告漏洞

如果你发现了安全漏洞，**请不要在公开 Issue 中披露**。

请通过以下方式私下报告：

- 📧 邮件给维护者（见 [README.md](README.md) 联系方式）
- 或开一个私密 Security Advisory（GitHub 仓库 → Security → Advisories → New draft）

请在报告中包含：

1. 漏洞的简要描述
2. 复现步骤（含 PoC 代码/截图）
3. 影响范围
4. 建议的修复方案（如有）

我们会在 **48 小时内**回复确认，并按以下流程处理：

1. 确认漏洞并评估严重度
2. 内部修复并写安全公告
3. 在新版本中发布修复
4. 在 [CHANGELOG.md](CHANGELOG.md) 公开致谢（如报告者同意）

## 已知安全考虑

本 Skill 调用以下外部工具，部署时需注意：

| 工具 | 风险 | 缓解措施 |
|------|------|----------|
| `yt-dlp` | 下载第三方视频，潜在恶意文件 | 限定到 `.mp4`、`.mp3`、`.wav`、`.m4a` 白名单 |
| `whisper.cpp` | 处理用户提供的音频 | 在沙箱中运行，不联网 |
| `ffmpeg` | 处理用户视频 | 限制输入文件大小 < 500MB |
| `Bash` 工具 | 任意命令执行风险 | 在 WorkBuddy 中使用 `allowed-tools` 限定 |

## 安全最佳实践

部署方应：

- ✅ 在 `allowed-tools` 中收紧到 `Bash(yt-dlp:*), Bash(ffmpeg:*), Bash(whisper:*)` 等前缀
- ✅ 输入视频先经病毒扫描
- ✅ 敏感词扫描在沙箱中独立运行
- ✅ 关闭 whisper 的网络访问
- ❌ 不要在生产环境以 root/admin 权限运行

## 致谢

感谢以下安全研究者的负责任披露（按时间顺序）：

_（暂无）_
